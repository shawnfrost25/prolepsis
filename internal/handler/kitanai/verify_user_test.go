package kitanai_test

import "testing"

func TestVerifyRegistration(t *testing.T) {
	t.Skip("Skipping this test, because it requires the token given by the email, which is sent via the function itself, meaning we cannot manipulate it and send a mock-token!!")
}
